package com.netcetera.threeds.sdk.infrastructure;

import android.os.SystemClock;
import android.view.KeyEvent;
import android.view.View;
import android.view.ViewConfiguration;
import java.util.Map;

/* JADX INFO: Access modifiers changed from: package-private */
/* loaded from: classes5.dex */
public class setNetworkAvailable implements setAlwaysDrawnWithCacheEnabled {
    private static int get = 1;
    private static int values;
    private final InterfaceC9641ns ThreeDS2ServiceInstance = C9642nt.ThreeDS2ServiceInstance(setNetworkAvailable.class);

    @Override // com.netcetera.threeds.sdk.infrastructure.setAlwaysDrawnWithCacheEnabled
    public void ThreeDS2ServiceInstance(setLayoutTransition setlayouttransition) {
        values = (get + 3) % 128;
        View.resolveSizeAndState(0, 0, 0);
        setlayouttransition.valueOf();
        setlayouttransition.get();
        setlayouttransition.getSDKInfo();
        Map<String, String> ThreeDS2ServiceInstance = setlayouttransition.ThreeDS2ServiceInstance();
        for (String str : ThreeDS2ServiceInstance.keySet()) {
            get = (values + 59) % 128;
            KeyEvent.getMaxKeyCode();
            ThreeDS2ServiceInstance.get(str);
        }
        if (!(!setlayouttransition.values())) {
            setlayouttransition.ThreeDS2Service().ThreeDS2ServiceInstance();
            get = (values + 25) % 128;
        } else {
            setlayouttransition.getWarnings().ThreeDS2ServiceInstance();
        }
        ViewConfiguration.getLongPressTimeout();
        setlayouttransition.ThreeDS2Service().ThreeDS2Service();
    }

    @Override // com.netcetera.threeds.sdk.infrastructure.setAlwaysDrawnWithCacheEnabled
    public void values(setOnHierarchyChangeListener setonhierarchychangelistener) {
        values = (get + 49) % 128;
        ViewConfiguration.getMinimumFlingVelocity();
        setonhierarchychangelistener.ThreeDS2ServiceInstance();
        setonhierarchychangelistener.values();
        Map<String, String> map = setonhierarchychangelistener.get();
        values = (get + 57) % 128;
        for (String str : map.keySet()) {
            SystemClock.uptimeMillis();
            map.get(str);
        }
        setonhierarchychangelistener.valueOf().ThreeDS2ServiceInstance();
        KeyEvent.getDeadChar(0, 0);
        setonhierarchychangelistener.ThreeDS2ServiceInstance();
        setonhierarchychangelistener.values();
    }
}